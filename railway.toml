[build]
builder = "nixpacks"
buildCommand = "npx drizzle-kit push --force && npm run build"

[deploy]
startCommand = "npm start"
healthcheckPath = "/api/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[service]
internalPort = 3000
