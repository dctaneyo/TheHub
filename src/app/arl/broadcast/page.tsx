"use client";

/**
 * The broadcast route is handled as an overlay by the layout's BroadcastStudio.
 * This page is a placeholder that the layout detects via activeView === "broadcast".
 * The actual BroadcastStudio component renders full-viewport from the layout.
 */
export default function BroadcastPage() {
  return null;
}
